# .github/workflows/mkdocs-docker.yml
name: MkDocs Deploy (Docker)

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # 允许推送 gh-pages
    steps:
      - uses: actions/checkout@v4

      - name: Build MkDocs image
        run: docker build -t mkdocs-site .

      - name: Deploy with mkdocs gh-deploy
        env:
          # 供 git 识别作者身份
          GIT_AUTHOR_NAME: github-actions
          GIT_AUTHOR_EMAIL: github-actions@users.noreply.github.com
          GIT_COMMITTER_NAME: github-actions
          GIT_COMMITTER_EMAIL: github-actions@users.noreply.github.com
        run: |
          docker run --rm \
            -v "$PWD":/docs \
            -w /docs \
            mkdocs-site sh -c '
              git config --global --add safe.directory /docs &&
              git config --global user.name  "$GIT_AUTHOR_NAME" &&
              git config --global user.email "$GIT_AUTHOR_EMAIL" &&
              mkdocs gh-deploy --force
            '